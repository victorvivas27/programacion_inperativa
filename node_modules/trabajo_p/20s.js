// Ejercicio 1
// En un concurso de fotografía, donde los usuarios publican una fotografía y obtienen
// likes, se registra la cantidad de likes obtenidos por cada usuario en un array. Ordenar
// los valores para poder indicar cuál fue la mayor cantidad de likes obtenidos, cuánto
// obtuvo el segundo, cuánto el tercero y cuánto el que menos likes obtuvo —suponer
// que participaron 15 usuarios y suponer para cada uno, una cantidad de likes—.
const likes = [120, 56, 300, 78, 90, 200, 450, 75, 100, 240, 320, 190, 80, 150, 50];

// Ordenar el array con Bubble sort 
for (let i = 0; i < likes.length; i++) {
    for (let j = 0; j < likes.length - 1; j++) {
        if (likes[i] > likes[j]) {
            let aux = likes[i];
            likes[i] = likes[j];
            likes[j] = aux;
        }
    }

}
const primerLugar = likes[0];
const segundoLugar = likes[1];
const tercerLugar = likes[2];
const ultimoLugar = likes[likes.length - 1];

// Imprimir los resultados
console.log("Primer lugar:", primerLugar);
console.log("Segundo lugar:", segundoLugar);
console.log("Tercer lugar:", tercerLugar);
console.log("Último lugar:", ultimoLugar);



// Ejercicio 2
// El servicio meteorológico, para llevar el control diario de temperatura, utiliza un
// objeto temperatura donde registra día —valor numérico del día—, mes —valor
// numérico—, temperatura máxima y temperatura mínima, correspondiente a dicho
// día. Las temperaturas —objeto temperatura— están cargados en un array.
// a) Ordenar por temperatura mínima de menor a mayor.
// b) Ordenar por temperatura máxima de mayor a menor.

const temperatura = [
    { dia: 1, mes: 1, tempMax: 32, tempMin: 18 },
    { dia: 2, mes: 1, tempMax: 28, tempMin: 14 },
    { dia: 3, mes: 1, tempMax: 25, tempMin: 12 },
    { dia: 4, mes: 1, tempMax: 30, tempMin: 17 },
    { dia: 5, mes: 1, tempMax: 27, tempMin: 13 },
    { dia: 6, mes: 1, tempMax: 23, tempMin: 10 },
    { dia: 7, mes: 1, tempMax: 26, tempMin: 15 },
];

// Ordenar por temperatura mínima de menor a mayor
for (let i = 0; i < temperatura.length; i++) {
    for (let j = 0; j < temperatura.length - 1; j++) {
        if (temperatura[j].tempMin > temperatura[j + 1].tempMin) {
            // Intercambiar los objetos
            const aux = temperatura[j];
            temperatura[j] = temperatura[j + 1];
            temperatura[j + 1] = aux;
        }
    }
}

// Imprimir el resultado
console.log("Ordenado por temperatura mínima de menor a mayor:");
console.table(temperatura);

// Ordenar por temperatura máxima de mayor a menor
for (let i = 0; i < temperatura.length; i++) {
    for (let j = 0; j < temperatura.length - 1; j++) {
        if (temperatura[j].tempMax < temperatura[j + 1].tempMax) {
            // Intercambiar los objetos
            const aux = temperatura[j];
            temperatura[j] = temperatura[j + 1];
            temperatura[j + 1] = aux;
        }
    }
}

// Imprimir el resultado
console.log("Ordenado por temperatura máxima de mayor a menor:");
console.table(temperatura);