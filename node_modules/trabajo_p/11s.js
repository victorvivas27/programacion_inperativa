/*et numeros = [1, 2, 3, 4, 5, 6]

let numeros2 = [1, 2, 3, 4, 5, 6, 1, 2, 3, 4, 5, 6]

function imparesPares(arrayNumeros) {
    let impares = []
    let pares = []
    for (let i = 0; i < arrayNumeros.length; i++) {
        if (arrayNumeros[i] % 2 === 0) {
            pares.push(arrayNumeros[i])
        } else {
            impares.push(arrayNumeros[i])
        }
    }

    return {
        impares: impares,
        pares: pares
    }
}


console.log(imparesPares(numeros))
console.log(imparesPares(numeros2))
/*Nos solicitan también crear un objeto literal llamado “banco”, el cual tendrá una
propiedad llamada “clientes” que estará compuesta de la lista de objetos
obtenidos en el punto anterior.*/
//la lista de clientes.
let arrayCuentas = [
    {
        nroCuenta: 5486273622,
        tipoDeCuenta: "Cuenta Corriente",
        saldoEnPesos: 27771,
        titularCuenta: "Abigael Natte",
    },
    {
        nroCuenta: 1183971869,
        tipoDeCuenta: "Caja de Ahorro",
        saldoEnPesos: 8675,
        titularCuenta: "Ramon Connell",
    },
    {
        nroCuenta: 9582019689,
        tipoDeCuenta: "Caja de Ahorro",
        saldoEnPesos: 27363,
        titularCuenta: "Jarret Lafuente",
    },
    {
        nroCuenta: 1761924656,
        tipoDeCuenta: "Cuenta Corriente",
        saldoEnPesos: 32415,
        titularCuenta: "Ansel Ardley",
    },
    {
        nroCuenta: 7401971607,
        tipoDeCuenta: "Cuenta Unica",
        saldoEnPesos: 18789,
        titularCuenta: "Jacki Shurmer",
    },
]
// podes continuar tu codigo a partir de aca!


const banco = {
    clientes: arrayCuentas,
consultarCliente: function (titular) {
        for (let i = 0; i < this.clientes.length; i++) {
            if (this.clientes[i].titularCuenta === titular) {
                return this.clientes[i]
            }
        }

        return null

    }


}
let clienteEncontrado = banco.consultarCliente('Ramon Connell');

//console.log(banco.consultarCliente('Jacki Chan'))
console.log(clienteEncontrado)
/*Crear otro método llamado depósito que recibirá dos parámetros: el titular de
cuenta y una cantidad de dinero a depositar. El método debe obtener la cuenta
correspondiente y luego sumar la cantidad de dinero a depositar a saldo en pesos.
Luego, deberá dar un aviso por la consola con el mensaje “Depósito realizado, su
nuevo saldo es: XXXX”.*/
function deposito(titular, cantidad) {
    // Buscamos la cuenta correspondiente al titular
    const cuenta = arrayCuentas.find(cuenta => cuenta.titularCuenta === titular);
  
    if (cuenta) {
      // Si se encuentra la cuenta, se realiza el depósito
      cuenta.saldoEnPesos += cantidad;
      console.log(`Depósito realizado, su nuevo saldo es: ${cuenta.saldoEnPesos}`);
    } else {
      // Si no se encuentra la cuenta, se muestra un mensaje de error
      console.log(`No se encontró la cuenta correspondiente al titular "${titular}"`);
    }
  }
  console.log(deposito('Ramon Connell',5000))