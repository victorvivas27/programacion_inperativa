//A partir de el siguiente array de edades nos solicitan realizar lo siguiente:
const edades = [11, 12, 15, 18, 25, 22, 10, 33, 18, 5];
//1Obtener en un nuevo array las edades menores a 18.
const menores = []
for (let i = 0; i < edades.length; i++) {
    if (edades[i] < 18) {
        menores.push(edades[i])
    }
}
//console.log(menores) // [11, 12, 15, 10, 5]
//Obtener en un nuevo array las edades mayor o igual a 18.
const mayores = [];
for (let i = 0; i < edades.length; i++) {
    if (edades[i] >= 18) {
        mayores.push(edades[i])
    }
}
//console.log(mayores) // [18, 25, 22, 33, 18]
//Obtener en un nuevo array las edades igual a 18.
const edadesIguales = []
for (let i = 0; i < edades.length; i++) {
    if (edades[i] === 18) {
        edadesIguales.push(edades[i])
    }
}
//console.log(edadesIguales)//[18,18]
//Obtener el menor.
//Obtener el mayor.
let menorEdad = edades[0];
let mayorEdad = edades[0];

for (let i = 1; i < edades.length; i++) {
    if (edades[i] < menorEdad) {
        menorEdad = edades[i];
    }
    else if (edades[i] > mayorEdad) {
        mayorEdad = edades[i];
    }
}
//console.log(menorEdad)
//console.log(mayorEdad)
//Obtener el promedio de edades.

let sumaEdades = 0
for (let i = 0; i < edades.length; i++) {
    sumaEdades += edades[i]
}
const promedioEdades = sumaEdades / edades.length
//console.log("sumaEdades:", sumaEdades)
//console.log("edades.length:", edades.length)
//console.log("promedioEdades:", promedioEdades)

//Incrementar en 1 todas las edades.
const edadesIncrementadas = [];
for (let i = 0; i < edades.length; i++) {
    edadesIncrementadas.push(edades[i] + 1);
}
//console.table(edades)
//console.table(edadesIncrementadas)

/*Tenemos como base un array de cuentas bancarias, donde a cada una la representamos con un objeto literal. A partir de este array trabajaremos sobre los siguientes enunciados, resolviendo de la forma que nos parezca más adecuada*/
const arrayCuentas =
    [
        {
            titular: "Arlene Barr",
            estaHabilitada: false,
            saldo: 3253.40,
            edadTitular: 33,
            tipoCuenta: "sueldo"
        },
        {
            titular: "Roslyn Torres",
            estaHabilitada: false,
            saldo: 3229.45,
            edadTitular: 27,
            tipoCuenta: "sueldo"
        },
        {
            titular: "Cleo Lopez",
            estaHabilitada: true,
            saldo: 1360.19,
            edadTitular: 34,
            tipoCuenta: "corriente"
        },
        {
            titular: "Daniel Malone",
            estaHabilitada: true,
            saldo: 3627.12,
            edadTitular: 30,
            tipoCuenta: "sueldo"
        },
        {
            titular: "Ethel Leon",
            estaHabilitada: true,
            saldo: 1616.52,
            edadTitular: 34,
            tipoCuenta: "sueldo"
        },
        {
            titular: "Harding Mitchell",
            estaHabilitada: true,
            saldo: 1408.68,
            edadTitular: 25,
            tipoCuenta: "corriente"
        }
    ]

//1)Obtener un nuevo array de cuentas cuyas edades sean menores a 30.
const cuentasMenoresDe30 = []
for (let i = 0; i < arrayCuentas.length; i++) {
    let cuenta = arrayCuentas[i]
    if (cuenta.edadTitular < 30) {
        cuentasMenoresDe30.push(cuenta)
    }
}
//console.log(cuentasMenoresDe30)
//2)Obtener un nuevo array de cuentas cuyas edades sean mayor o igual a 30.

const cuentasMayoresDe30 = []

for (let i = 0; i < arrayCuentas.length; i++) {
    let cuenta = arrayCuentas[i]
    if (cuenta.edadTitular >= 30) {
        cuentasMayoresDe30.push(cuenta)
    }
}

//console.log(cuentasMayoresDe30)
//3)Obtener un nuevo array de cuentas cuyas edades sean menores o igual a 30.

const cuentasMenoresIgualDe30 = []
for (let i = 0; i < arrayCuentas.length; i++) {
    let cuenta = arrayCuentas[i]
    if (cuenta.edadTitular <= 30) {
        cuentasMenoresIgualDe30.push(cuenta)
    }
}
//console.log(cuentasMenoresIgualDe30)
//4)Obtener la cuenta con el titular de la misma más joven.
let cuentaMasJoven = arrayCuentas[0]
for (let i = 1; i < arrayCuentas.length; i++) {
    if (arrayCuentas[i].edadTitular < cuentaMasJoven.edadTitular) {
        cuentaMasJoven = arrayCuentas[i]
    }
}
//console.log(cuentaMasJoven)

//5)Obtener un array con las cuentas habilitadas.
const cuentasHabilitadas = []
for (let i = 0; i < arrayCuentas.length; i++) {
    if (arrayCuentas[i].estaHabilitada) {
        cuentasHabilitadas.push(arrayCuentas[i])
    }
}
//console.log(cuentasHabilitadas)
//6)Obtener un array con las cuentas deshabilitadas.
const cuentasDeshabilitadas = []
for (let i = 0; i < arrayCuentas.length; i++) {
    if (arrayCuentas[i].estaHabilitada === false) {
        cuentasDeshabilitadas.push(arrayCuentas[i])
    }
}
//console.log(cuentasDeshabilitadas)

//7)Obtener la cuenta con el menor saldo.
//8)Obtener la cuenta con el mayor saldo.
let cuentaMenorSaldo = arrayCuentas[0]
let cuentaMayorSaldo = arrayCuentas[0]

for (let i = 1; i < arrayCuentas.length; i++) {
    if (arrayCuentas[i].saldo < cuentaMenorSaldo.saldo) {
        cuentaMenorSaldo = arrayCuentas[i]
    }
    else if (arrayCuentas[i].saldo > cuentaMayorSaldo.saldo) {
        cuentaMayorSaldo = arrayCuentas[i]
    }
}

//console.log("Cuenta con el menor saldo:", cuentaMenorSaldo)
//console.log("Cuenta con el mayor saldo:", cuentaMayorSaldo)
//EXTRA:
//1)Desarrollar una función llamada generarID que reciba como parámetro el array de cuentas y agregue a cada elemento (objeto literal) una propiedad llamada id con un valor Numérico
function generarID(arrayCuentas) {
    for (let i = 0; i < arrayCuentas.length; i++) {
        arrayCuentas[i].id = i + 1
    }
}
generarID(arrayCuentas)
console.log(arrayCuentas)

/*2)Desarrollar una función llamada buscarPorId que reciba como parámetro el array de cuentas y un id, en caso de encontrar retornar la cuenta (él objeto literal completo), caso contrario retornar null*/
function buscarPorId(arrayCuentas, id) {
    for (let i = 0; i < arrayCuentas.length; i++) {
        if (arrayCuentas[i].id === id) {
            return arrayCuentas[i]
        }
    }
    return null
}
//console.log(buscarPorId(arrayCuentas,5))

/*3)Desarrollar una función llamada filtrarPorSaldos que reciba como parámetro el array de cuentas y un saldo (Number), deberá retornar un array que se cuyas cuentas se encuentren por debajo del saldo enviado por parámetro*/
function filtrarPorSaldos(arrayCuentas, saldoMaximo) {
    const cuentasFiltradas = []
    for (let i = 0; i < arrayCuentas.length; i++) {
        if (arrayCuentas[i].saldo < saldoMaximo) {
            cuentasFiltradas.push(arrayCuentas[i]);
        }
    }
    return cuentasFiltradas;
}


  //console.log(filtrarPorSaldos( arrayCuentas, 2000))
  /*4)Desarrollar una función llamada incrementarSaldo que reciba como parámetro el array de cuentas, un id y un saldo, deberá incrementar él saldo de la cuenta, en caso de encontrar, caso contrario retornar undefined Reutilizando la función  buscarPorId*/

  function incrementarSaldo(arrayCuentas, id, saldoIncremento) {
    let  cuentaEncontrada = buscarPorId(arrayCuentas, id);
    if (cuentaEncontrada) {
      cuentaEncontrada.saldo += saldoIncremento;
      return cuentaEncontrada;
    }
    return undefined;
  }
  
  // Ejemplo de uso
  const cuentaIncrementada = incrementarSaldo(arrayCuentas, 2, 500);
  console.log(cuentaIncrementada);