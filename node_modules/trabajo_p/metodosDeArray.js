let horariosPartida = [12, 14, 18, 21];
const horariosAtrasados = horariosPartida.map(function (numero) {
    return numero + 1
})
//console.log(horariosAtrasados);
// Necesitamos enviarle un diploma a los alumnos que están aprobados y un e-mail a los alumnos desaprobados para acordar un recuperatorio. Por lo tanto, necesitamos dos listas: una lista que almacene todos los aprobados y otra los desaprobados. Deberemos guardarlos en las variables aprobados y desaprobados respectivamente.
let estudiantes = [
    { nombre: 'John', promedio: 8.5, aprobado: true },
    { nombre: 'Jane', promedio: 7, aprobado: true },
    { nombre: 'June', promedio: 3, aprobado: false },
]

// Utilizamos el método filter para separar a los estudiantes aprobados y desaprobados
const aprobados = estudiantes.filter(estudiante => estudiante.aprobado);
const desaprobados = estudiantes.filter(estudiante => !estudiante.aprobado);

// Mostramos los resultados por consola
console.log('Estudiantes aprobados:', aprobados);
console.log('Estudiantes desaprobados:', desaprobados);
// Para este ejercicio tenemos el array listaDeSupermercado. Nuestro trabajo será mostrar cada ítem de esa lista por consola utilizando un foreach.
let listaDeSuperMercado = [
    'Bife de chorizo',
    'Coca Cola',
    'Bananas',
    'Lechuga',
    'Chimichurri',
    'Lata de tomates',
    'Arvejas',
    'Cereales',
    'Pechuga de pollo',
    'Leche'
];
listaDeSuperMercado.forEach(function (lista) {
    console.log(lista)
})