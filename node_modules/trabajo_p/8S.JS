

//1)CREAR  una función que pida un valor por parámetro y muestre los 10
//números siguientes.
function mostrarSiguientes10Numeros(numero) {
    for (let num = numero + 0; num <= numero + 10; num++) {
        console.log(num)
    }
}
//mostrarSiguientes10Numeros(10)


//2) Imprimir los números entre el 5 y el 20, saltando de tres en tres.
function imprimirNumeros(numero) {
    for (let i = numero; i <= 20; i += 3) {
        console.log(i)
    }
}
//imprimirNumeros(5)

//3) Escribir un programa que muestre la sumatoria de todos los números
//entre el 0 y el 100
let suma = 0
for (let i = 0; i <= 100; i++) {
    suma += i
}
//console.log(`La sumatoria de los números entre 0 y 100 es ${suma}`)

//4) Dado un número entero positivo, mostrar su factorial. El factorial de
//un número se obtiene multiplicando todos los números enteros
//positivos que hay entre el 1 y ese número.
function factorial(numero) {
    let resultado = 1
    for (let i = 1; i <= numero; i++) {
        resultado *= i
    }
    return resultado
}

//console.log(factorial(3)); // Output: 120
//5) Crear una función que muestre todos los números de la secuencia de
//fibonacci hasta el valor ingresado por parámetro.
function fibonacci(numero) {
    let a = 0
    let b = 1
    let fibonacciArray = [a, b];
    while (b <= numero) {
        let temp = b
        b = a + b
        a = temp
        fibonacciArray.push(b)
    }
    return fibonacciArray
}


console.log(fibonacci(100))
// La secuencia de Fibonacci es una serie de números en la que cada número es la suma de los dos números anteriores. Comienza con 0 y 1, y luego los siguientes números se generan sumando los dos números anteriores.

// En el código, la función fibonacci() toma un parámetro numero que es el límite superior de la secuencia de Fibonacci que se generará.

// Se inicializan dos variables a y b en 0 y 1, respectivamente, que representan los primeros dos números en la secuencia de Fibonacci.
// Se crea una matriz fibonacciArray que contiene estos dos números iniciales.
// A continuación, se usa un bucle while para agregar números adicionales a la matriz fibonacciArray hasta que el número b sea mayor que el parámetro numero.
// Dentro del bucle, se crea una variable temporal temp que se establece en b. Luego, b se actualiza a la suma de a y b, y a se actualiza a temp. Esto genera el siguiente número en la secuencia de Fibonacci.
// El nuevo número generado (b) se agrega a la matriz fibonacciArray.
// Finalmente, cuando se sale del bucle, la función devuelve la matriz fibonacciArray completa que contiene los números de Fibonacci hasta el número dado.