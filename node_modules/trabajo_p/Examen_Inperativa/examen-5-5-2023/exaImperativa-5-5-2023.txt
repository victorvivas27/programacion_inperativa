// IMPORTANTE: Realiza el correspondiente llamado a cada una de las funciones para demostrar el correcto funcionamiento de las mismas.
//Ejercicio 1
//Dado él siguiente array 

let arrayDeNumeros = [1, 2, 3, 8, 9, 104, 5, 6, 7, 15]

//Crear una función que reciba como parámetros el array la misma deberá retornar un objeto literal con dos propiedades
const separarParesImpares = (array) => {
    let pares = []
    let impares = []
    for (let i = 0; i < array.length; i++) {
        if (array[i] % 2 === 0) {
            pares.push(array[i])
        } else {
            impares.push(array[i])
        }
    }
    return {
        pares: pares,
        impares: impares
    }
}
//console.log(separarParesImpares(arrayDeNumeros))
// { pares: [ 2, 8, 104, 6 ], impares: [ 1, 3, 9, 5, 7, 15 ] }


//Ejercicio 2
//Dado él siguiente array de paises
let paises = [
    {
        nombre: "Argentina",
        continente: "Sudamérica",
        poblacion: 40000
    },
    {
        nombre: "Colombia",
        continente: "Sudamérica",
        poblacion: 50372
    },

    {
        nombre: "Brasil",
        continente: "Sudamérica",
        poblacion: 300000
    },
    {
        nombre: "Etiopía",
        continente: "África",
        poblacion: 15000
    },
    {
        nombre: "Chile",
        continente: "Sudamérica",
        poblacion: 10000
    }
]

// Crear una función llamada bubbleSort que reciba como parámetros el array de países, deberá realizar un ordenamiento ascendente según la población, es decir, de menor a mayor
// Para analizar el correcto funcionamiento imprimir por consola el array antes y después de ejecutar la función.

const bubbleSort = (array) => {
    for (let i = 0; i < array.length; i++) {
        for (let j = 0; j < array.length - 1; j++) {
            if (array[j].poblacion > array[j + 1].poblacion) {
                let aux = array[j]
                array[j] = array[j + 1]
                array[j + 1] = aux
            }
        }
    }
}
// console.log("Array antes de la función:")
// console.log(paises)

// bubbleSort(paises)

// console.log("Array después de la función:")
// console.log(paises)

//Ejercicio 3
//Dada la matriz 
let matriz = [
    [5, 2, 2], // 0
    [2, 5, 2], // 1
    [4, 4, 5]  // 2
]
// Crear una función que reciba la matriz por parámetro, multiplique todos los valores de primer diagonal  y retorne él valor.
// Crear una función que reciba la matriz generada por parámetro y retorne un nuevo array con todos los valores impares de la matriz.

//Función para multiplicar los valores de la primera diagonal de una matriz cuadrada
const multiplicaDiagonal = (matriz) => {
    let resultado = 1
    for (let i = 0; i < matriz.length; i++) {
        resultado = resultado * matriz[i][i]
    }
    return resultado
}
console.log(multiplicaDiagonal(matriz)) // Resultado esperado: 125

//Función para obtener un array con todos los valores impares de una matriz
const valoresImpares = (matriz) => {
    let impares = []
    for (let i = 0; i < matriz.length; i++) {
        for (let j = 0; j < matriz[i].length; j++) {
            if (matriz[i][j] % 2 === 1) {
                impares.push(matriz[i][j])
            }
        }
    }
    return impares
}
//console.log(valoresImpares(matriz)) // Resultado esperado: [5, 5, 5]