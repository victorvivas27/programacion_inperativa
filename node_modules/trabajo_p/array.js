/*exerciseProgressIcon
Ejercicio 2: pop()
¡Sí! ¡Alexis se egresó del terciario!
Para este ejercicio contamos con el array estudiantes, en donde, en cada índice, almacena un objeto con las propiedades nombre, promedio y curso.
Queremos sacar a Alexis, que acaba de egresar, y sabemos que está último en ese array.
Para eso debemos, utilizando un método de array, guardar los datos de Alexis en una variable nueva llamada alumnoEgresados.*/
let estudiantes = [
  {
    nombre: "Alvaro",
    promedio: 9,
    curso: "Android"
  },
  {
    nombre: "Daniel",
    promedio: 6,
    curso: "Full Stack"
  },
  {
    nombre: "Alexis",
    promedio: 3,
    curso: "iOS"
  }

]
let alumnoEgresados = estudiantes.pop()

estudiantes.length
console.log(estudiantes)
console.log(alumnoEgresados)
/*Ejercicio 3: push()
Se inscribieron dos estudiantes nuevos y queremos agregarlos al final del array estudiantes.
Nuestro trabajo será agregar de a un alumno por vez, utilizando algún método de array.
Los valores de cada uno son:
nombre: Juan
promedio: 5
curso: iOS
------------------
nombre: Miguel
promedio: 2
curso: Android*/
let estudiantes1 = [
  {
    nombre: 'Alvaro',
    promedio: 9,
    curso: 'Android',
  },
  {
    nombre: 'Daniel',
    promedio: 6,
    curso: 'Full Stack',
  },
  {
    nombre: 'Alexis',
    promedio: 3,
    curso: 'iOS',
  },
]
estudiantes1.push(
  {
    nombre: 'Juan',
    promedio: 5,
    curso: 'iOS',
  },
  {
    nombre: 'Miguel',
    promedio: 2,
    curso: 'Android',
  }
)
console.table(estudiantes1)

//EJEMPLO DE length
let mensaje ='Gran dia para practicar Javascript'
console.log(mensaje)
console.log(mensaje.length)
//EJEMPLO DE indexOf
console.log(mensaje.indexOf('dia'))
//EJEMPLO DE slice
console.log(mensaje.slice(0,24))
//EJEMPLO DE trim
console.log(mensaje.trim())
//EJEMPLO DE split
console.log(mensaje.split(' '))
//EJEMPLO DE replece
console.log(mensaje.replace('Javascript','Todo'))



//let frases = 'Breaking Bad Rules'
//frases.slice(9,12)
//console.log(frases)
//let textoReemplazado = texto.replace(/mala/g, 'buena')
/*function reemplazoFastFast(texto, buscar, reemplazar) {
    let textoReemplazado = texto.replace(new RegExp(buscar, 'g'), reemplazar);
    return textoReemplazado
  }
  let textoSinGuiones = reemplazoFastFast('Este texto es mala onda','mala','buena');
console.log(textoSinGuiones) // 'Este texto es buena onda'

function menciona(texto, palabra) {
    return texto.includes(palabra)
  }
 
  function menciona(texto, palabra) {
    return texto.indexOf(palabra) !== -1
  }
  console.log(menciona('Existen muchos lenguajes de programación y JavaScript es uno de ellos','programación')) // true
  let numeros = [1, 2, 3, 4, 5, 6]

function imparesPares() {
  let impares = [];
  let pares = [];
  for (let i = 1; i <= numeros.length; i++) {
    if (numeros[i] % 2 == 0){
      impares.push(i);
    } else {
      pares.push(i);
    } 
  }
  return {impares, pares};
}
console.log(imparesPares())
function separarEdades(edades) {
  let edadesMenor = [];
  let edadesMayor = [];
  
  for(let i = 0; i < edades.length; i++) {
    if(edades[i] < 18) {
      edadesMenor.push(edades[i]);
    } else {
      edadesMayor.push(edades[i]);
    }
  }
  
  return {
    edadesMenor: edadesMenor,
    edadesMayor: edadesMayor
  };
}
function tieneDuplicados(array) {
  for (let i = 0; i < array.length; i++) {
    for (let j = i + 1; j < array.length; j++) {
      if (array[i] === array[j]) {
        return true;
      }
    }
  }
  return false;
}

let edad4 = [2, 3, 4, 5, 6, 4];
console.log(tieneDuplicados(edad4)); // true*/
